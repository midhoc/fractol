
int main()
{
    int r = 255,g=0,b=0;
    int i=1525;
     while(i -= 10 > 0){
    if ( r >= 255 && g < 255 && b <= 0 )
      {
      g+=10;
      }
    if ( g >= 255 && r > 0 && b <= 0 )
      {
      r-=10;
      }
    if ( g >= 255 && b < 255 && r <= 0 )
      {
      b+=10;
      }
    if ( b >= 255 && g > 0 && r <= 0 )
      {
      g-=10;
      }
    if ( b >= 255 && r < 255 && g <= 0 )
      {
      r+=10;
      }
    if ( r >= 255 && b > 0 && g <= 0 )
      {
      b-=10;
      }
      i++;
    printf("%d\t%d\t%d\t%d\n",r,g,b,i);
      if (r>= 255 && g<=0 && b <= 0)
        return(0);
    }
}
////////////1525